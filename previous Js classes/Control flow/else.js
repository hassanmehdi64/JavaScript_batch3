let games = 50

if (games == 60) {
    console.log(`games are not playing`);
}

else  {
    console.log(`game are playing`);
}

const rooms = 50

if (rooms >60) {
    console.log(`rooms ar not available`);
}

else {
    console.log(`rooms are available`);
}

let hotels = 100

if (hotels >100) {
    console.log(`hotels are full`);
}

else {
    console.log(`hotels are not full`);
}

const bikes = 500

if (bikes >600) {
    console.log(`bikes are very slow`);
}

else {
    console.log(`bikes are very fast`);
}